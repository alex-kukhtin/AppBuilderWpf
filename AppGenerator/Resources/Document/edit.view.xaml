<!--$(SchemaName).$(ModelName) Index -->
<Page xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml" Padding=".5rem"
	  Title="{Bind $(ElementName).Memo, Format='Document {0}'}">
	<Page.Toolbar>
		<Toolbar>
			<Button Icon="Save" Command="{BindCmd Save}"/>
			<Button Icon="Close" Command="{BindCmd Close}"/>
		</Toolbar>
	</Page.Toolbar>
		<Grid>
		<Label Content="{Bind $(ElementName).Id}"></Label>
			<TextBox 
				Label="Memo" Value="{Bind $(ElementName).Memo}" Multiline="True" Rows="3"/>
	</Grid>
</Page>